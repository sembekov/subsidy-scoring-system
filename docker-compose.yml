services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: project-frontend-1
    ports:
      - "80:3000"
    stdin_open: true
    tty: true
    environment:
      - HOST=0.0.0.0
    command: npm start
